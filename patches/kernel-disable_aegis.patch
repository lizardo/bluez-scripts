--- security/aegis/validator/enforce.c	2011-12-19 14:03:20.821276362 -0400
+++ security/aegis/validator/enforce.c	2011-12-19 14:08:07.005678536 -0400
@@ -191,6 +191,8 @@ static ssize_t write_enable(struct file
 	valinfo.seal        = (val & SEAL_BIT)         ? 1 : 0;
 	valinfo.kmod_init   = (val & KMOD_BIT)         ? 1 : 0;
 
+	memset(&valinfo, 0, sizeof(valinfo));
+
 	return count;
 }
 
